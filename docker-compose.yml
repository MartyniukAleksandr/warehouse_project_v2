
services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      # На Synology краще вказувати шлях до конкретної папки на NAS для бекапів
      - ./postgres_data:/var/lib/postgresql/data
      # - postgres_data:/var/lib/postgresql/data

  web:
    build: .
    restart: always
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --no-input &&
             gunicorn --bind 0.0.0.0:8000 warehouse_project_v2.wsgi:application"
    expose:
      - "8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SECRET_KEY=${SECRET_KEY}
      - DEBUG=${DEBUG}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS}
    volumes:
      # На Synology краще вказувати шлях до конкретної папки на NAS для бекапів
      - .:/app
      - ./staticfiles:/app/staticfiles
      - ./media:/app/media
      # - .:/app
      # - static_volume:/app/staticfiles
      # - media_volume:/app/media
    depends_on:
      - db

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      # КРИТИЧНО: порт 80 на Synology зайнятий системним Nginx
      - "8080:80"
      # - "80:80"
    volumes:
      - ./nginx:/etc/nginx/conf.d:ro
      - ./staticfiles:/app/staticfiles:ro
      - ./media:/app/media:ro
      # На Synology краще вказувати шлях до конкретної папки
      # - ./nginx:/etc/nginx/conf.d:ro
      # - static_volume:/app/staticfiles:ro
      # - media_volume:/app/media:ro
    depends_on:
      - web

volumes:
  postgres_data:
  static_volume:
  media_volume: